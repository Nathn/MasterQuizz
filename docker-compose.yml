version: '3'
services:
  web:
    build:
      context: .
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      WS_PORT: 3001

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
